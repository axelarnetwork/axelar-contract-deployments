use super::Processor;

impl Processor {
    /// Some description here.
    pub fn deploy_token_manager_b() {
        todo!()
    }
}

// function interchainTokenId(address sender, bytes32 salt) public pure returns
// (bytes32 tokenId) {     tokenId =
// keccak256(abi.encode(PREFIX_INTERCHAIN_TOKEN_ID, sender, salt)); }
