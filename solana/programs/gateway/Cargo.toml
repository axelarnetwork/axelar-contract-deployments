[package]
name = "gateway"
version = "0.1.0"
description = "Axelar Gateway implementation for Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "gateway"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = {version = "0.28.0"}
borsh = {version = "0.10.3"}

registry = { path = "../registry", features = ["cpi"]}