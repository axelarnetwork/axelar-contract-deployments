# Security Configuration
# Ensure @aikidosec/safe-chain is used for security scanning

# Enable strict SSL for secure package downloads
strict-ssl=true

# Save exact versions to prevent unexpected updates
save-exact=true

# Only use the official npm registry to avoid supply chain attacks
registry=https://registry.npmjs.org/

# Audit packages during installation (note: safe-chain provides additional scanning)
audit-level=moderate

# Use lockfile version 3 for better dependency resolution
lockfile-version=3
