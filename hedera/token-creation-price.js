'use strict';

const TINY_PARTS_PER_WHOLE = 100_000_000;

const DEFAULT_TOKEN_CREATION_PRICE_USD = 1;
const DEFAULT_TOKEN_CREATION_PRICE_TINY_CENTS = DEFAULT_TOKEN_CREATION_PRICE_USD * TINY_PARTS_PER_WHOLE;

module.exports = {
    TINY_PARTS_PER_WHOLE,
    DEFAULT_TOKEN_CREATION_PRICE_USD,
    DEFAULT_TOKEN_CREATION_PRICE_TINY_CENTS,
};

// TODO(hedera) allow changing the token creation price via CLI
