/**
 * Global constants for the Axelar deployment process
 */

// Base URL for downloading contract files
export const BASE_URL = 'https://static.axelar.network/releases/cosmwasm';

// Directory to store WASM files
export const WASM_DIR = '../wasm';

// Directory to store configuration files
export const CONFIG_DIR = './configs';

// Common wallet name for operations
export const WALLET_NAME = 'amplifier';

// Standard gas price coefficient
export const GAS_PRICE_COEFFICIENT = '0.00005';